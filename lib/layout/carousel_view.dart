import 'package:flutter/material.dart';

class AnimeCarouselView extends StatelessWidget {
  const AnimeCarouselView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}